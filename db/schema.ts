import { boolean, integer, jsonb, pgTable, primaryKey, text, timestamp, uniqueIndex } from "drizzle-orm/pg-core";
export const repositoryInstallations = pgTable("comment_lens_repository_installation", { id: text("id").primaryKey(), installationId: integer("installation_id").notNull(), ownerLogin: text("owner_login").notNull(), ownerId: text("owner_id").notNull(), repositoryId: text("repository_id").notNull(), repository: text("repository").notNull(), defaultBranch: text("default_branch").notNull(), isPrivate: boolean("is_private").notNull().default(false), lastVerifiedAt: timestamp("last_verified_at", { withTimezone: true }), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), expiresAt: timestamp("expires_at", { withTimezone: true }) }, (table) => [uniqueIndex("comment_lens_installation_repo_idx").on(table.installationId, table.repositoryId)]);
export const scans = pgTable("comment_lens_scan", { id: text("id").primaryKey(), owner: text("owner"), ownerId: text("owner_id").notNull(), ownerLogin: text("owner_login").notNull(), repositoryInstallationId: text("repository_installation_id").notNull().references(() => repositoryInstallations.id), repository: text("repository").notNull(), ref: text("ref").notNull(), headCommit: text("head_commit"), resolvedCommit: text("resolved_commit"), status: text("status").notNull(), workflowRunId: text("workflow_run_id"), workflowDispatchId: text("workflow_dispatch_id"), configHash: text("config_hash"), extractorVersion: text("extractor_version"), parserVersions: jsonb("parser_versions").notNull().default({}), commentCount: integer("comment_count").notNull().default(0), fileCount: integer("file_count").notNull().default(0), chunkCount: integer("chunk_count").notNull().default(0), contentSha256: text("content_sha256"), diagnostics: jsonb("diagnostics").notNull().default([]), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), completedAt: timestamp("completed_at", { withTimezone: true }), retentionUntil: timestamp("retention_until", { withTimezone: true }) });
export const commentIdentities = pgTable("comment_lens_comment_identity", { id: text("id").primaryKey(), repositoryInstallationId: text("repository_installation_id").notNull().references(() => repositoryInstallations.id), path: text("path").notNull(), kind: text("kind").notNull(), normalizedBody: text("normalized_body").notNull(), symbolAnchor: text("symbol_anchor"), placement: text("placement").notNull(), collisionIndex: integer("collision_index").notNull().default(0), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() });
export const comments = pgTable("comment_lens_comment", { id: text("id").notNull(), scanId: text("scan_id").notNull().references(() => scans.id), identityId: text("identity_id").notNull().references(() => commentIdentities.id), path: text("path").notNull(), language: text("language"), kind: text("kind").notNull(), placement: text("placement").notNull(), bodyText: text("body_text").notNull(), rawText: text("raw_text").notNull(), context: text("context"), payload: jsonb("payload").notNull(), priorityScore: integer("priority_score"), stale: boolean("stale").notNull().default(false), protected: boolean("protected").notNull().default(false), generated: boolean("generated").notNull().default(false), license: boolean("license").notNull().default(false), todoOnly: boolean("todo_only").notNull().default(false), authorName: text("author_name"), authorEmail: text("author_email"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() }, (table) => [primaryKey({ columns: [table.scanId, table.id] }), uniqueIndex("comment_lens_scan_identity_idx").on(table.scanId, table.identityId)]);
export const reviewDecisions = pgTable("comment_lens_review", { commentId: text("comment_id"), identityId: text("identity_id").primaryKey().references(() => commentIdentities.id), ownerId: text("owner_id"), status: text("status").notNull(), note: text("note"), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() });
export const chunks = pgTable("comment_lens_chunk", { scanId: text("scan_id").notNull().references(() => scans.id), chunkId: text("chunk_id").notNull(), sequence: integer("sequence").notNull(), totalChunks: integer("total_chunks").notNull(), payloadSha256: text("payload_sha256").notNull(), payload: jsonb("payload").notNull(), receivedAt: timestamp("received_at", { withTimezone: true }).notNull().defaultNow() }, (table) => [primaryKey({ columns: [table.scanId, table.chunkId] }), uniqueIndex("comment_lens_chunk_sequence_idx").on(table.scanId, table.sequence)]);
export const assessments = pgTable("comment_lens_assessment", { commentId: text("comment_id"), identityId: text("identity_id").primaryKey().references(() => commentIdentities.id), ownerId: text("owner_id"), providerId: text("provider_id").notNull(), model: text("model").notNull(), promptVersion: text("prompt_version").notNull(), contextScope: text("context_scope").notNull(), payload: jsonb("payload").notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), retentionUntil: timestamp("retention_until", { withTimezone: true }) });
