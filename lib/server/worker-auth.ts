import { createHmac, timingSafeEqual } from "node:crypto";
export function verifyWorkerRequest(raw: string, signature: string | null, timestamp: string | null) { const secret = process.env.WORKER_INGEST_SECRET; if (!secret || !signature || !timestamp) return false; const time = Number(timestamp); if (!Number.isFinite(time) || Math.abs(Date.now() - time) > 300000) return false; const expected = createHmac("sha256", secret).update(timestamp + "." + raw).digest("hex"); return signature.startsWith("sha256=") && signature.slice(7).length === expected.length && timingSafeEqual(Buffer.from(signature.slice(7)), Buffer.from(expected)); }
