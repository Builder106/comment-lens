import { NextResponse } from "next/server";
import { cookies } from "next/headers";
import { setSession } from "../../../../../lib/server/auth";
import { jsonError } from "../../../../../lib/server/http";
export async function GET(request: Request) { const url = new URL(request.url); const code = url.searchParams.get("code"); const state = url.searchParams.get("state"); const store = await cookies(); if (!code || !state || state !== store.get("comment_lens_oauth_state")?.value) return jsonError("invalid_request", "Invalid OAuth state", 400); const clientId = process.env.GITHUB_APP_CLIENT_ID; const clientSecret = process.env.GITHUB_APP_CLIENT_SECRET; if (!clientId || !clientSecret) return jsonError("internal_error", "GitHub OAuth is not configured", 503); const tokenResponse = await fetch("https://github.com/login/oauth/access_token", { method: "POST", headers: { accept: "application/json", "content-type": "application/json" }, body: JSON.stringify({ client_id: clientId, client_secret: clientSecret, code }), cache: "no-store" }); if (!tokenResponse.ok) return jsonError("internal_error", "GitHub token exchange failed", 502); const token = (await tokenResponse.json()) as { access_token?: string }; if (!token.access_token) return jsonError("internal_error", "GitHub did not return an access token", 502); const profileResponse = await fetch("https://api.github.com/user", { headers: { authorization: `Bearer ${token.access_token}`, accept: "application/vnd.github+json", "user-agent": "comment-lens" }, cache: "no-store" }); if (!profileResponse.ok) return jsonError("internal_error", "GitHub profile lookup failed", 502); const profile = (await profileResponse.json()) as { id?: number; login?: string }; if (!profile.login || profile.id === undefined) return jsonError("internal_error", "GitHub profile is invalid", 502); try { store.delete("comment_lens_oauth_state"); store.set(setSession(profile.login, String(profile.id))); } catch { return jsonError("forbidden", "GitHub account is not authorized", 403); } return NextResponse.redirect(new URL("/", request.url)); }
